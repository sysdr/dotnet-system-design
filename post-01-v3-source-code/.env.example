# .env.example — Post 1
# Copy to .env before running Docker Compose: Copy-Item .env.example .env
# Not needed when running with .NET Aspire AppHost — Aspire sets env vars automatically.
ASPNETCORE_ENVIRONMENT=Development
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
